{
  "name": "lighthouse-github-action",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test jest --coverage --silent",
    "test:watch": "NODE_ENV=test jest --watch --notify --notifyMode=change",
    "start": "node src/index.js"
  },
  "jest": {
    "collectCoverageFrom": [
      "src/*.{js}"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.2",
    "chrome-launcher": "^0.10.7",
    "fs-extra": "^8.0.1",
    "lighthouse": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^24.8.0"
  }
}
